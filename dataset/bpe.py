from collections import Counter, defaultdict

def splits_sentences(sentence):
    tokens = sentence.split()
    

def compute_pair_freqs(split):
    pair_freqs = defaultdict(int)
    for word, freq in sp